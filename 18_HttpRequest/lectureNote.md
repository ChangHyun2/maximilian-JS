<script> (client side)
    - frontend , detached from backend
    - fetching, sending data
Server side
    - run on different server
    - storing & retrieving data (with database)

client와 server를 연결하는 이동 수단 : HTTP
이동에 필요한 경로 URL(domain + path)
도착 후 다음 동작은? : http method

HTTP request /response
URL(domain + path)

url : naverm.com/posts
domain : naver.com
path : /posts


HTTP를 통한 통신에서 허용되는 format
JSON format
FormData format
Binary etc...
